# Rock, Paper, Scissors Game 🪨📄✂️

A classic command-line implementation of the Rock, Paper, Scissors game written in Python. This project demonstrates basic control flow, user input handling, and the use of the `random` library.

## 🚀 How to Play
1. Run the script in your terminal.
2. When prompted, type your choice: `rock`, `paper`, or `scissors`.
3. The computer will randomly select its move.
4. The winner is determined based on standard game rules.
5. Choose `y` to play again or any other key to exit.

## 🛠️ Built With
* **Python 3**
* **Random Module**

## 💻 How to Run
Make sure you have Python installed, then run:
```bash
python main.py